next : 완료

*1-10단계학습법 적용
*참고.
  1-6단계, 목표, 계획, 학습데이터 모으는 단계
  7단계(대충 사용 수준까지)
  8단계(이것저것해보기)
  9단계(유용한 것을 할수있는 수준까지 파기)
  10단계(가르치기)

주제 : kernel interrupt 초기화로직과 interrupt 발생시 주요 로직 흐름(=인터럽트핸들러 수행까지의 흐름)

학습이유
  호기심(interrupt발생시 커널이 뭔일을 하는지가 궁금)
  low-level지식습득.

학습목표
  kernel interrupt 초기화로직 파악(주요로직수준)
  interrupt 발생시 주요 로직 흐름(=인터럽트핸들러 수행까지의 흐름) (주요로직수준)
    
학습자료
  이것저것 많다.!

학습계획
  milestone1. kernel interrupt 초기화로직 및 인터럽트핸들러 수행까지 흐름에 대한 주요 로직 학습.
  
================================================================
7단계 *목표 :  kernel interrupt 초기화로직 / interrupt 발생시 주요 로직 흐름(=인터럽트핸들러 수행까지의 흐름) 파악.
7-1. kernel interrupt 초기화 흐름 *external interrupt의 경우.(익셉션핸들러도 비슷한 흐름.)
step1) external interrupt에 대한 IDT엔트리 구현
  external interrupt핸들러 array(irq_entries_start) 구현 *$(top)/arch/x86/include/asm/idtentry.h line 519-546
  external interrupt 핸들러에 의해 호출되는 공통 asm 함수(asm_common_interrupt) 구현 *$(top)/arch/x86/entry/entry_64.S line 569-582
  위 공통asm함수에의해 호출되는 공통C함수(common_interrupt, __common_interrupt) 구현 *(DEFINE_IDTENTRY_*매크로, $(top)/arch/x86/include/asm/idtentry.h에선언됨.)
step2) external interrupt 핸들러에(irq_entires_start) 대한 IDT 초기화.
  external interrupt에 대한 IDT디스크립터 초기화 수행. *idt_setup_apic_and_irq_gates()에의해 수행.

external interrupt 처리하는 공통 함수들 호출 순서
  external 인터럽트 핸들러(irq_entries_start + vector number) -> asm_common_interrupt -> common_interrupt -> __common_interrupt -> irq handler*여기서는 다루지 않았음(ex. driver에서 등록(ex. cp_interrupt))
  *$(top)/arch/x86/kernel/irq.c line 236-264 참조.

용어
  Interrupt : CPU에서 발생하는 Interrupt
  Exception : CPU에서 발생하는 Exception
  Interrupt Handler : Interrupt발생시 수행되는 Handler
  Exception Handler : Exception발생시 수행되는 Handler
  Interrupt/Exception vector/vector number/number : Interrupt/Exception에 할당된 고유의 id(숫자)
  IDT : 인터럽트 디스크립터 테이블. IDT Descriptor들의 테이블.
  IDT Descriptor/(Interrupt)Gate Descriptor : 인터럽트 디스크립터. Interrupt/Exception Handler의 주소를 포함
  IDT Entry : 해당 IDT와 연관된 엔트리포인트.(ex. function). 즉 Interrupt/Exception Handler.

$(top)/arch/x86/include/asm/idtentry.h
인터럽트 엔트리를 정의하는 DECLARE_IDTENTY*() 동일매크로에 대해 C level, ASM level 두벌 정의되어있음.

line 8-460
  C level DECLARE_IDTENTY*()관련 매크로 정의(__ASSEMBLY__ 매크로로 구분됨)
  C level function 정의로 expansion됨.(#define참조)

line 461-565
  ASM level DECLARE_IDTENTY*()관련 매크로 정의(__ASSEMBLY__ 매크로로 구분됨)
  최종적으로 .macro asm directive를 통해 expansion됨. *$(top)/arch/x86/entry/entry_64.S line 351-389 참조

line 519-546
  irq_entries_start symbol
  모든 external interrupt들에 대한 entry를 정의
    interrupt vector number를 stack에 push하고(line 539, irq_entries_start symbol정의 주석참조)
    asm_common_interrupt로 jmp.(line 567-749에 의해서 정의됨.)
  idt_setup_apic_and_irq_gates()에 의해 IDT에 등록된다.

line 567-749
  인터럽트핸들러 및 인터럽트 C level 함수들을 정의 또는 선언 by DECLARE_IDTENTY*().
  C level컴파일이냐, ASM level컴파일이냐에 따라 동일매크로가 다르게 expansion됨.
    C level 컴파일 : 인터럽트핸들러 및 C level함수 선언(prototype만 선언)
    ASM level 컴파일 : 인터럽트핸들러 정의.(실제 인터럽트핸들러 구현) *$(top)/arch/x86/entry/entry_64.S line 351-389 참조
  external interrupt의 경우 asm_common_interrupt(ASM level), common_interrupt(C level)을 선언함.
  
$(top)/arch/x86/entry/entry_64.S
line 318-349
  idtentry_body
    line 333
      interrupt 처리를 위한 stackframe세팅(by error_entry)
      *ALTERNATIVE macro : 특정 조건의 경우 oldinstruction대신 newinstruction으로 치환하는 매크로. 상세한건 skip...
    line 336-337
      skip(debug/보안관련 정보)
    line 339
      C function에 전달할 1st arg세팅(스택포인터(rsp), 즉 pt_regs)
    line 341-344
      C function에 전달할 2nd arg세팅(error code *external interrupt의 경우 interrupt vector number)
    line 346
      C function 호출
      external interrupt의 경우, common_interrupt가 호출됨. $(top)/arch/x86/include/asm/idtentry.h line 567-749참조.
    line 348
      return(by error_return)

line 351-389
  idtentry macro
    인터럽트핸들러를 통해 호출되는 공통 asm 함수를 정의하는 매크로. 
    external interrupt의 경우, asm_common_interrupt symbol로 정의됨. $(top)/arch/x86/include/asm/idtentry.h 분석내용참조.
    line 363-365 *skip.
    line 367-383
      external interrupt일때는 의미없음(동작X)
       대략, interrupt발생시 stack에 필요한 값을 push함.
      *line 371-383은 skip. external interrupt인 경우 의미없음
    line 385
      C function호출환경 셋업후 호출한다.(by idtentry_body)
      
line 569-582
  __irqentry_text_start, __irqentry_text_end 심볼
  인터럽트핸들러들이 위치하게됨 $(top)/arch/x86/include/asm/idtentry.h line 567-749 에 정의된 심볼.

line 1005-1100
  error_entry
    interrupt처리를 위한 stack frame을 setup한다. stack frame(pt_regs)는 %rax에 저장되게 되는듯(?)
    line 1011
      stack에 register들을 모두 push (by PUSH_AND_CLEAR_REGS macro)
      *이전에 H/W적으로 CS등이 push됨. (x86 ISA spec), 64bit mode인 경우, 64bit fixed size로 push(ex. CS는 16bit지만 64bit으로 push)
    line 1014-1015
      스택에 저장된 CS를통해 kernel로부터 진입되었는지 user로부터 진입되었는지 체크
      이후 적절한 분기를 수행
    line 1021
      gs register를 swap. 자세한건 skip...
    line 1028
      C function으로 전달한 아규먼트(pt_regs의 포인터)를 세팅.
      *rsp에 offset +8을 이유 : rsi register가 추가로 저장되기때문임(by line 1011)
    line 1029-1066
      크게 아래 두가지 케이스로 나뉨. 아마, privilege level이 달라질때 stack switch발생하는 것 관련일 듯.(Intel64 and IA-32 Architectures Software Developer's Manual, Volume3, 6.12.1참조  *자세한건 skip)
     user mode로부터 진입 *line 1029-1033
       현재 cpu의 stack에 line 1011에의해 push된 stack프래임(pr_regs)값을 copy.(by sync_regs *$(top)/arch/x86/kernel/traps.c line 714-720)
       이후 return수행
     kernel mode로부터 진입 *line 1041-1091
       return address가 native_irq_return_iret인지 체크. 즉 native_irq_return_iret실행중 인터럽트 발생했는지를 확인하는 듯.
        체크결과에따라 아래 둘 중 하나의 분기를 탐. *자세한 조건은 skip...
         1. 함수리턴값을 pt_regs(line 1011에 의해 push됨)로 세팅 *line 1064-1066
        2. .Lerror_entry_from_usermode_after_swapgs로 jmp해서 기능 재수행. 즉 line 1029-1033을 수행함.
             
    참고
      지금 단계에서는 중요하지 않아 skip한 macro들(디버깅, 커널보안 등의 목적임)
        UNWIND_HINT_FUNC
        ENCODE_FRAME_POINTER
        FENCE_SWAPGS_USER_ENTRY
        SWITCH_TO_KERNEL_CR3   
        IBRS_ENTER
        UNTRAIN_RET

line 1094-1100
  error_return
    user mode에서 진입되었는 지 여부에따라(line 1097)
    kernel return(line 1098, by restore_regs_and_return_to_kernel)
    user return(line 1099, by swapgs_restore_regs_and_return_to_usermode)
    를 수행
    *swapgs_restore_regs_and_return_to_usermode는 kernel_systemcall.txt 해당 분석 내용 참조
    *restore_regs_and_return_to_kernel은 swapgs_restore_regs_and_return_to_usermode이랑거의비슷한데 단순히 POP_REGS만 수행한다는 차이가 있음.

$(top)/arch/x86/kernel/idt.c
line 158-159
  idt_table
    IDT를 정의

line 161-164
  idt_descr
    IDTR에 load될 정보. 쉽게말해 IDTR정보.
    IDT로 idt_table을 사용함. *line 158-159참조. 

line 179-190
  idt_setup_from_table()
    IDT에 IDT Descriptor정보를 세팅한다.(by idt_init_desc, write_idt_entry)

line 192-199
  set_intr_gate()
    IDT에 IDT Descriptor정보를 세팅한다.(by init_idt_data, idt_setup_from_table)

line 264-298
  idt_setup_apic_and_irq_gates()
    인터럽트엔트리에 대한 IDT들을 세팅.(ex. irq_entries_start symbol을 참조)
    커널 초기화 시점에 수행됨.(init_IRQ에 의해)
    line 272
      IDT에 APIC관련 IDT descriptor를 세팅한다.(by idt_setup_from_table)
    line 274-277
      IDT에 external interrupt 관련 IDT descriptor를 세팅한다.(by set_intr_gate). *irq_entries_start 참조.
      *for_each_clear_bit_from(bit, addr, size) : addr이 가리키는 memory에서, 
                                                  bit번째부터 clear(=0)된 bit를 찾는다. 
                                                  size(bit갯수)번 loop를 돈다.
    line 279-289
      local apic관련 인터럽트(spurious interrupt)들을 추가 세팅한다. 상세한건 skip.
    line 290-295
      IDTR에 정보(idt_descr) load(by load_idt).
      *idt_map_in_cea(), set_memory_ro()는 skip(IDT와 크게 무관하므로 일단 skip)
    line 297
      idt_setup_done flag세팅(true)
      
    
$(top)/arch/x86/include/asm/desc_defs.h
line 77-82
  struct idt_data
    IDT descriptor 정보를 가지고 있는 구조체
    실제 IDS descriptor와 1:1매칭되는것은 아님. 실제 매칭은 struct gate_struct.

line 84-93
  struct gate_struct
    IDT descriptor 구조체.
    x86의 IDT descriptor 를 의미.

$(top)/arch/x86/include/asm/desc.h
line 407-418
  init_idt_data()
    struct idt_data 필드를 초기화.

line 420-432
  idt_init_desc()
    IDT descriptor(struct gate_struct) 구조체값을 세팅(by struct idt_data)

$(top)/arch/x86/kernel/irq.c
line 236-264 *#define macro로 아래 2개 함수(common_interrupt, __common_interrupt)로 치환되어 아래처럼 분석진행함.
  common_interrupt()
    얘는 특별히 $(top)/arch/x86/include/asm/idtentry.h line기준.
    line 211
      interrupt vector정보를 가져옴.
    line 215
      __func호출(by run_irq_on_irqstack_cond)
      external interrupt의 경우 __common_interrupt함수 호출.
    참고
      irqentry_*()함수 skip : interrupt tracing관련(디버깅) 정보인 듯.
      instrumentation_*()함수 skip : stack validation 정보인 듯.
      kvm_*()함수 skip : kvm관련은 굳이 볼 필요는 없겠다...
    
  __common_interrupt()
    line 242
       해당 cpu의 인터럽트처리에 interrupt stack frame정보를 셋업(by set_irq_regs)
    line 248-251
      driver등에의해 등록된 irq handler를 수행(by handle_irq)
      *7-2 calltrace참조
       *자세한건 skip : 여기부터는 irq_desc, irqaction 영역이고 코드가 너무 다량이므로 별도 주제로 분리.
    line 251-261
       에러처리. 자세한건 skip(여기부터는 irq_desc, irqaction 영역이고 코드가 너무 다량이므로 별도 주제로 분리)
    line 263
      line 242작업 원복(by set_irq_regs)
    
    참고
      DEFINE_IDTENTRY_IRQ 매크로를 통해 common_interrupt, __common_interrupt 함수정의
        *($(top)/arch/x86/include/asm/idtentry.h line 192-220참조)
      external interrupt의 경우, 인터럽트핸들러 -> asm_common_interrupt -> common_interrupt -> __common_interrupt가 호출됨.
        인터럽트핸들러 : external interrupt핸들러
                     *$(top)/arch/x86/include/asm/idtentry.h line 519-546참조.
        asm_common_interrupt : external interrupt 핸들러에 의해 호출되는 공통 asm 함수. 
                               *$(top)/arch/x86/include/asm/idtentry.h line 567-749참조.
        common_interrupt : DEFINE_IDTENTRY_IRQ매크로에의해 expansion되는 함수.
        __common_interrupt : DEFINE_IDTENTRY_IRQ매크로에의해 expansion되는 함수. 이건 구현하는 사람이 직접 짬.

$(top)/arch/x86/include/asm/irq_stack.h
line 128-131
  call_on_irqstack_cond
    인자로넘어온 func을 call함.
    external interrupt의 경우, __common_interrupt를 call함.
    별도 macro가 존재하는 이유는 케이스(user영역으로부터 넘어왔는지, 아닌지)에 따라 함수호출하기 전에 필요한 셋업동작이 다름.
    line 133-141
      user mode로부터 넘어오거나(stack에 push된 cs register의 rpl값이 3인지를 체크), hardirq stack이 사용중인 경우에 실행되며 
       인자로넘어온 func를 수행함. external interrupt의 경우 __common_interrupt를 call함.
    line 141-151
      line 133-141과 비슷한 시퀀스를 수행.
       1. 스택포인터(rsp)를 irq stack(hardirq_stack_ptr 변수)로 세팅
      2. line 133-141과 동일기능을 수행(irq_enter_rcu ~ call function ~ irq_exit_rcu) *external interrup인 경우 __common_interrupt를 call
       3. 스택포인터를 원복
      *call_on_stack주석참조
    참고
     hardirq stack? *추측
       kernel실행중 interrupt발생시, interrupt처리를 위해 임시로 사용하는 intrrupt stack
        인터럽트 중첩등이 고려된 듯.
        즉 user실행중 interrupt발생 또는 이미 hardirq stack이 사용중이면 -> hardirq stack 사용안함
        그 외 경우 interrupt발생 -> 사용함
     irq_enter_rcu, irq_exit_rcu() : kernel tracing/accounting용 정보인듯. 일단 skip     

line 195-203
  run_irq_on_irqstack_cond()
    인자로 넘어온 func를 수행함.(bu call_on_irqstack_cond)

$(top)/include/asm-generic/irq_regs.h
line 13-17
  irq_regs
    가장 최근 interrupt stack frame(struct pt_regs) 정보를 가지고 있는 per-cpu variable.
    get_irq_regs(), set_irq_regs()로 해당 값을 참조

7-2. interrupt발생시 핸들러수행까지의 주요 흐름.
cp_interrupt함수(realtek ethernet driver interrupt 처리함수) 콜트래이스 일부.(ftrace 사용)
             cat-5209    [002] d.h..  3705.149534: irq_handler_entry: irq=11 name=enp0s3         => cat실행중, 인터럽트발생(irq=11, enp0s3)
             cat-5209    [002] d.h..  3705.149535: cp_interrupt <-__handle_irq_event_percpu      => 인터럽트처리 시점의 스택트래이스
             cat-5209    [002] d.h..  3705.149536: <stack trace>
 => 0xffffffffc011c083
 => cp_interrupt                                                                                 =>
 => __handle_irq_event_percpu
 => handle_irq_event_percpu                                                                         handle_irq에 의해 irq handler를 호출(__common_interrupt 분석내용 참조)
 => handle_irq_event                                                                                8139cp driver(realtek ethernet drvier)의 경우임.
 => handle_fasteoi_irq                                                                           => 
 => __common_interrupt                                                                           =>
 => common_interrupt                                                                                asm_common_interrupt -> common_interrupt -> __common_interrupt 호출!
 => asm_common_interrupt                                                                         => 
 => queue_work_on
 => tty_insert_flip_string_and_push_buffer
 => n_tty_write
 => file_tty_write.isra.0
 => new_sync_write
 => vfs_write
 => ksys_write
 => do_syscall_64
 => entry_SYSCALL_64_after_hwframe
             cat-5209    [002] d.h..  3705.149561: irq_handler_exit: irq=11 ret=handled          => 인터럽트 핸들러 종료

8단계 *목표 : 7단계 부족했던 부분 추가 학습 + 이것저것해보기
  (v)idtentry macro 코드분석 $(top)/arch/x86/entry/entry_64.S line 351-389 참조
  (v)__common_interrupt 코드 분석 $(top)/arch/x86/kernel/irq.c line 236-264 참조
  (v)idt_setup_apic_and_irq_gates 코드분석 $(top)/arch/x86/kernel/idt.c line 264-298 참조

9단계 *목표 : 디버그로그추가해서 분석내용 확인.(공부한 내용 수준으로는 먼가 직접 추가는 어려워보임.)
  커널코드수정해서, idt_setup_apic_and_irq_gates에서 interrupt handler셋업 확인.
  출력결과. 분석한대로 동작함.!
knw@knw-Standard-PC-i440FX-PIIX-1996:~$ dmesg | grep knwtest
[    0.068115] knwtest init_IRQ!
[    0.068123] knwtest native_init_IRQ!
[    0.068226] knwtest idt_setup_apic_and_irq_gates!
[    0.068228] knwtest vector:33, entry:(____ptrval____)
[    0.068229] knwtest vector:34, entry:(____ptrval____)
[    0.068230] knwtest vector:35, entry:(____ptrval____)
[    0.068230] knwtest vector:36, entry:(____ptrval____)
[    0.068231] knwtest vector:37, entry:(____ptrval____)
...
10단계 *목표 : 정리
  알게된 것
    external interrupt의 low-level관점에서의 구현.
    external interrupt의 low-level관점에서의 큰 흐름.(초기화시점과, 인터럽트핸들러 호출 흐름)
  코멘트
    개인사정(논다고...)으로 루즈했고, 일정사유로(대박황금연휴) 어떻게든 끊을려고하다보니 얻은게 딱히 없는 듯...
    low-level 지식(x86 architecture, 어셈블리)은 여전히 더 필요하다...
