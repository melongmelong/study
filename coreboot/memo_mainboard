next : 완료

*1-10단계학습법 적용
*참고.
  1-6단계, 목표, 계획, 학습데이터 모으는 단계
  7단계(대충 사용 수준까지)
  8단계(이것저것해보기)
  9단계(유용한 것을 할수있는 수준까지 파기)
  10단계(가르치기)

주제 : coreboot 오픈소스 분석 - coreboot에서 mainboard 지원하기 위한 방식을 확인

학습이유
  coreboot 오픈소스 분석
    low-level 지식 향상
    오픈소스 프로젝트 참여
    => low-level 커리어 up!
  coreboot에서 mainboard 지원하는 동방식을 확인
  
학습목표
  $(top)/src/mainboard/emulation/qemu-i440fx 각 소스코드의 의미 확인
  $(top)/src/mainboard/emulation/qemu-i440fx코드가 coreboot에 integration되는 방식을 확인
  새로운 mainboard를 추가할 경우, 어떻게 처리해야하는 지 내용 정리(기본개념이라도)
    
학습자료
  coreboot 공식홈페이지 doc(https:/

학습계획
  milestone1. coreboot에서 mainboard 지원하기 위한 방식을 확인
  
=======================
milestone1. coreboot에서 mainboard 지원하기 위한 방식을 확인

7단계(대충 사용 수준까지) - qemu-i440fx 메인보드 지원방식과 coreboot에 mainboard추가 방식 확인.

mainboard qemu-i440fx 디렉토리 소스코드
  devicetree.cb : devicetree파일
  bootmode.c : VBOOT 지원을 위한 코드. $(top)/src/security/vboot/vboot_logic.c에서 사용됨.
                 모든 stage에 포함.(all-y make변수에 추가됨) $(top)/src/mainboard/emulation/qemu-i440fx/Makefile.mk 참조
  irq_table.c : irq routing table 셋업하는 로직. $(top)/src/arch/x86/table.c에서 사용됨
                $(top)/src/arch/x86/Makefile.mk에서 해당 소스코드를 ramstage-y make변수에 추가
  mainboard.c : struct device_operations를 정의. devicetree와 연관. devicetree.cb로 부터 생성되는 $(top)/build/mainboard/emulation/qemu-i440fx/static.c에서 사용됨.
  memory.h : 함수선언만 존재. 같은 경로의 memmap.c에 #include 됨.
  romstage.c : romstage에서 mainboard entry 로직.
               $(top)/src/cpu/intel/car/romstage.c에서 참조함.
               $(top)/src/arch/x86/Makefile.mk, romstage-srcs에 추가됨.
  Kconfig/Kconfig.name : Kconfig파일
  Makefile.mk : makefile
  northbridge.c : struct chip_operations를 정의. devicetree와 연관. devicetree.cb로 부터 생성되는 $(top)/build/mainboard/emulation/qemu-i440fx/static.c에서 사용됨.
  exit_car.S : car stage에서 사용됨. 동일경로 Makefile.mk에 의해 postcar-y 에 추가됨.
  memmap.c : 메모리정보관련 로직. $(top)/src/lib/imd_cbmem.c 과 동일경로 다른 소스코드에서 사용됨
               동일경로 Makefile.mk에의해서 romstage-y, postcar-y, ramstage-y에 추가됨
  rom_media.c : ROM관련 로직. $(top)/src/lib/boot_device.c, $(top)/src/lib/fmap.c 에서 참조됨.
                  동일경로 Makefile.mk에 의해서 ramstage-y에 추가됨
  board_info.txt : 메인보드에 대한 정보성 파일
  acpi : ACPI정보. dsdt.asl에서 #include로 포함함. *cpu-hotplug.asl은 안쓰는 듯(???)
  cmos.default : CMOS정보. CBFS에 들어가나봄. $(top)/src/arch/x86/Kconfig 에 default값으로되어있음.
  cmos.layout : CMOS정보. CBFS에 들어가나봄. $(top)/src/Kconfig 에 default값으로되어있음. $(top)/src/arch/x86/Makefile.mk에 파일을 사용한 make rule있음. $(top)/src/lib/coreboot_table.c에서 cmos_layout.bin을 참조함.
    CMOS이란?
      BIOS setting을 저장한 nonvolatile memory.
      https://en.wikipedia.org/wiki/Nonvolatile_BIOS_memory
  vboot-rwa-8M.fmd : VBOOT 관련. *skip(https://doc.coreboot.org/security/vboot/index.html)
  dsdt.asl : ACPI정보. CBFS에 들어가나봄. $(top)/src/acpi/Makefile.mk에 dsdt빌드룰 만듬.asl_template매크로는 $(top)/Makefile.mk에 존재. $(top)/src/acpi/acpi.c, write_acpi_tables()에서 dsdt.aml을 참조함.
    ASL이란?
      ACPI Source Language (ASL).
      ASL is a source language for defining ACPI objects including writing ACPI control methods. OEMs and platform firmware developers define objects and write control methods in ASL and then use a translator tool (compiler) to generate ACPI Machine Language (AML) versions of the control methods (https://uefi.org/htmlspecs/ACPI_Spec_6_4_html/19_ASL_Reference/ACPI_Source_Language_Reference.html)
      iasl이라는 tool로 컴파일함.(asl -> aml).

mainboard qemu-i440fx 빌드 산출물
공통
  *.o, *.d 파일
    $(top)/src/mainboard/emulation/qemu-i440fx/Makefile.mk 에 의해 각 stage별 포함된 소스코드파일에 대한 컴파일 결과.
    static.o, static.d 는 $(top)/Makefile.mk line 782-808에 의해 거의 대부분 stage에 추가됨.
    irq_tables.o, irq_tables.d는 $(top)/src/arch/x86/Makefile.mk 에 의해 ramstage에 추가됨.
      
$(top)/build/mainboard/emulation/qemu-i440fx
  static.c : qemu-i440fx devicetree빌드결과. $(top)/Makefile.mk line 798에 빌드룰 존재
$(top)/build/ramstage/mainboard/emulation/qemu-i440fx
$(top)/build/postcar/mainboard/emulation/qemu-i440fx
$(top)/build/smm/mainboard/emulation/qemu-i440fx
$(top)/build/bootblock/mainboard/emulation/qemu-i440fx
$(top)/build/verstage/mainboard/emulation/qemu-i440fx
$(top)/build/romstage/mainboard/emulation/qemu-i440fx

mainboard에 coreboot포팅(=추가)방법.
  요점
    빌드시스템에추가 : Kconfig, Makefile.mk
    각 stage에서, 필요한 weak symbol을 정의하는 로직 구현. : *.c. grep __weak 으로 코드베이스 검색하면 함수들 확인 가능.
    devicetree 구성. : devicetree.cb, 
    ACPI 구성. : *.asl
    이 외 하드웨어초기화로직(ex gpio), struct chip_operations 같은 구조체 등도 연결하는 작업이 필요.
  참고. mainboard 포팅 편의를 위한 tool도 존재하는 것으로 보임 : $(top)/util/autoport
  
8단계(이것저것해보기) - 아래 잘 몰랐던 부분도 추가확인.

mainboard추가 프로세스 확인을 위한, 기존 mainboard관련 코드 샘플링.
결론 : 메 바 베(mainboard by mainboard)라서, 기본적인 틀이랑 구현된 코드 기반으로 잘 구현해야할 듯;;
$(top)/src/mainboard/intel/apollolake_rvp
  Kconfg / Kconfig.name
  Makefile.mk
  board_info.txt
  devicetree.cb
  dsdt.asl
  gpio.h
  bootblock.c
    bootblock_mainboard_early_init()
  romstage.c
    mainboard_memory_init_params()

$(top)/src/mainboard/intel/coffeelake_rvp
  Kconfig/Kconfig.name
  Makefile.mk
  board_info.txt
  dsdt.asl
  bootblock.c
    bootblock_mainboard_early_init()
  chromeos.c
  memory.c
  mainboard.c
    struct chip_operations mainboard_ops
  variants/
    baseboard/
      Makefile.mk
      devicetree.cb
      gpio.c
      nhlt.c

$(top)/src/mainboard/amd/bilby
  Kconfig/Kconfig.name
  Makefile.mk
  board_info.txt
  dsdt.asl
  devicetree.cb
  bootblock.c
    bootblock_mainboard_early_init()
  early_gpio.c
  gpio.h/gpio.c
  hda_verb.c
  mainboard.h/mainboard.c
    struct chip_operations mainboard_ops
  port_descriptors.c
  romstage.c
  
cmos.default, cmos.layout관계 파기.
  딱히 없어보인다. 일단 skip...

$(top)/build/mainboard/emulation/qemu-i440fx 의 cbfs*파일정체???? 어떻게 생성되나????
  make -p 1>$(path/to/log)로 빌드하면 path/to/log에 빌드룰 어떤게 실행되는 지 확인 가능. 그걸로 확인함.
  2가지종류가 생성됨
  - coreboot config 헤더
    build/mainboard/emulation/qemu-i440fx/cbfs-file.ioMhTt.out: /home/knw/Desktop/Study/coreboot/coreboot/src/coreboot/.config build/build.h build/util/kconfig/conf
    $(top)/Makefile.mk의 cbfs-files-processor-config 룰에 의해 생성
    $(top)/Makefile.mk의 'config-file := $(DOTCONFIG):config'에서 cbfs-files-handler의 tmp-cbfs-method를 정의(config)

  - cbfs_header
    build/mainboard/emulation/qemu-i440fx/cbfs-file.voo0oZ.out: src/lib/cbfs_master_header.c build/build.h build/fmap_config.h build/config.h
    $(top)/Makefile.mk의 cbfs-files-processor-struct 룰에 의해 생성
  둘다 cbfs에 들어감.
    $(top)/src/lib/Makefile.mk의 'cbfs_master_header-file := cbfs_master_header.c:struct'에서 cbfs-files-handler의 tmp-cbfs-method를 정의(struct)

   - 참고. coreboot Makefile에서의 cbfs-files 조작
     cbfs-files-y 변수에 file추가후 아래 변수들 셋팅됨.
       $(file)-file :=
       $(file)-type :=
       $(file)-postion :=
     이후 $(top)/Makefile.mk의 cbfs-files-handler에서 위 변수값을 참조하여 handler / 변수 등록.

    knw@knw-System-Product-Name:~/Desktop/Study/coreboot/coreboot/src/coreboot$ ./build/cbfstool ./build/coreboot.rom print
    FMAP REGION: COREBOOT
    Name                           Offset     Type           Size   Comp
    cbfs_master_header             0x0        cbfs header        32 none                       ====> 이거
    fallback/romstage              0x80       stage           19152 none
    fallback/ramstage              0x4bc0     stage           62779 LZMA (126640 decompressed)
    config                         0x14180    raw              2156 LZMA (6474 decompressed)   ====> 이거
    revision                       0x14a40    raw               773 none
    build_info                     0x14d80    raw               114 none
    fallback/dsdt.aml              0x14e40    raw              4073 none
    cmos_layout.bin                0x15e80    cmos_layout       640 none
    fallback/postcar               0x16140    stage           20328 none
    fallback/payload               0x1b100    simple elf      44131 none
    (empty)                        0x25dc0    null          4018660 none
    bootblock                      0x3fafc0   bootblock       16384 none
  
ACPI/ASL 파기(?) *이건안하는 게 맞다.

9단계(유용한 것을 할수있는 수준까지 파기) - qemu-i440fx 디버그로그찍어서 동작 확인 + qemu-i440fx 매인보드 로직 리팩토링같은걸로 commit
디버그로그찍었는데 특별한 insight는 얻을 수 없음...(그냥 호출되야할 때되면 호출됨...)
https://review.coreboot.org/c/coreboot/+/94842

10단계(가르치기) - 내용정리
  알게된것
    mainboard추가는 정답이없다. 여기저기 필요한 부분의 로직을 적절히 제어한다.
    포팅 제일 좋은 시작점은, 가장 비슷한 mainboard 또는 tool(autotool)이다.
  코멘트
    mainboard는 어렵다.
    더 정진합시다 아직 갈길이 멀다.
